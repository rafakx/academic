<%= form_for(@teacher_course) do |f| %>
  <% if @teacher_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher_course.errors.count, "error") %> prohibited this teacher_course from being saved:</h2>

      <ul>
      <% @teacher_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br>
    <%= f.number_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :course_type_id %><br>
    <%= f.number_field :course_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
